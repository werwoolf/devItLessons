import React from 'react';
import '../styles.scss'

const Loader = () => {
    return (
        <div className="loader">
            <div className="loader__element"></div>
        </div>
    );
};

export default Loader;